# Current Feature

Dashboard fase 2

## Status

<!-- Not Started|In Progress|Completed -->

In Progress

## Goals

- Implement phase 2/3 of the dashboard UI.
- Use `@lib/mockdata.ts` as the temporary data source.
- Make the left menu collapsible.
- Add a Drawer icon to open and close the sidebar.
- Keep the Drawer icon visible by default on mobile.
- Show item types in the menu with their corresponding icon and color.
- Add a collapsible collections section below item types with a `>` icon.
- Show the 3 latest created collections and a `view all` link in the collections section.
- Show the active user and avatar at the bottom of the menu.

## Notes

- References:
  - `@context/features/dashboard-2-spec.md`
  - `@context/stich/design-home.md`
  - `@context/stich/screens/mainScreen.png`
  - `@context/project-overview.md`
  - `@lib/mockdata.ts`
- The spec file title still says `Dashboard fase 1`, but its content clearly defines phase 2/3 and is treated as the source of truth.

## History

<!-- Keep this updated. Earliest to latest -->

- Dashboard fase 1: creada la ruta `/dashboard`, top bar con búsqueda y placeholders de `Menu` y `Main`.
- Dashboard fase 2 activada y marcada como `In Progress` usando `context/features/dashboard-2-spec.md` como referencia.
